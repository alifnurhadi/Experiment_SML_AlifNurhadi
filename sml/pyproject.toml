[project]
name = "sml"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.138.1",
    "mlflow>=3.14.0",
    "prometheus-client>=0.25.0",
    "psutil>=7.2.2",
    "uvicorn>=0.49.0",
]
